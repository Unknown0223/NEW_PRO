-- Skipped: original script referenced non-existent tables (warehouse, client, payments, visits, …).
-- Referential integrity is already defined in schema.prisma migrations; no-op for existing DBs.
SELECT 1;
