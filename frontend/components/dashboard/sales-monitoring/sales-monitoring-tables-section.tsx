"use client";

import type { ReactNode } from "react";

export function SalesMonitoringTablesSection({ children }: { children: ReactNode }) {
  return <div data-dashboard-section="sales-monitoring-tables">{children}</div>;
}
