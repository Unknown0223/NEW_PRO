import { redirect } from "next/navigation";

/** Eski dnd-kit konstruktor — Virtual Pivot ga yo'naltirish. */
export default function ReportBuilderLegacyPage() {
  redirect("/reports/builder/pivot");
}
