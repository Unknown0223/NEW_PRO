/**
 * Legacy / zakaz bo‘yicha polki: jami fizik dona (oplata + bonus omborga) bitta hujjatda.
 * Erkin «Создать возврат с полки» — faqat qator «макс. всего» (yetkazilgan savdo), hujjat 24 cheklovi yo‘q.
 * Backend: `MAX_RETURN_ITEMS` — explicit polki qatorlarida qo‘llanmaydi.
 */
export const MAX_RETURN_PHYSICAL_UNITS_PER_DOCUMENT = 24;
