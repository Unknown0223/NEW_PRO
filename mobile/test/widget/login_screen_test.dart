import 'package:flutter_test/flutter_test.dart';

void main() {
  group('LoginScreen', () {
    testWidgets('shows login form fields', (tester) async {
      // TODO: Wrap with ProviderScope and test
      expect(true, isTrue);
    });

    testWidgets('shows error on empty submit', (tester) async {
      // TODO: Test validation
      expect(true, isTrue);
    });
  });
}
