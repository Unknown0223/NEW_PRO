name: salesdoc_mobile
description: SalesDoc mobil ilova — agent, ekspeditor, supervayzer uchun
publish_to: "none"
version: 3.1.0+301

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  flutter_riverpod: ^2.4.9
  go_router: ^14.2.0
  flutter_secure_storage: ^9.0.0
  flutter_dotenv: ^5.1.0
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  logger: ^2.0.2
  connectivity_plus: ^6.0.0
  permission_handler: ^11.3.0
  shimmer: ^3.0.0
  intl: ^0.19.0
  path_provider: ^2.1.2
  path: ^1.9.0
  sqflite: ^2.3.3
  geolocator: ^13.0.0
  device_info_plus: ^11.3.0
  package_info_plus: ^8.0.0
  image_picker: ^1.0.7
  flutter_image_compress: ^2.1.0
  local_auth: ^2.3.0
  crypto: ^3.0.3
  webview_flutter: ^4.10.0
  url_launcher: ^6.3.2
  battery_plus: ^6.2.1
  flutter_local_notifications: ^18.0.1

  local_auth_android: any
  webview_flutter_android: any
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - .env.local
    - .env.production
